ccash_python_client
===================
Python client for CCash servers. Uses the `requests` library and translates
errors into exceptions from numbers.

The original porject source is [https://github.com/entiretwix/ccash].

installation
------------
Install pip with python and run the following command:

    python3 -m pip install ccash_python_client

